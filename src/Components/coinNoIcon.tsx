import { Typography } from '@material-ui/core';

const CoinNoIcon = () => {
  return (
    <div className="coin-noicon-placeholder">
      <Typography variant="subtitle1" className="coin-noicon-text gradient-text">?</Typography>
    </div>
  );
};

export default CoinNoIcon;